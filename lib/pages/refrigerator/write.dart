import 'package:flutter/material.dart';

class RefrigeratorWritePage extends StatelessWidget {
  const RefrigeratorWritePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('냉관'),
      ),
    );
  }
}
